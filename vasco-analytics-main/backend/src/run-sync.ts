import { PrismaClient } from '@prisma/client'; import { PrismaService } from './prisma/prisma.service'; import { EquipesService } from './equipes/equipes.service'; async function main() { const prismaService = new PrismaService(); const equipesService = new EquipesService(prismaService); const count = await equipesService.sincronizarEscudosLocais(); console.log('Escudos atualizados:', count); } main();
