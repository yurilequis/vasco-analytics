const { PrismaClient } = require('@prisma/client'); const p = new PrismaClient(); async function main() { const p1 = await p.jogador.findMany({ where: { nomePopular: { contains: 'Payet', mode: 'insensitive' } } }); console.log('Payets:', p1); const p2 = await p.jogador.findMany({ where: { nomePopular: { contains: 'Teixeira', mode: 'insensitive' } } }); console.log('Teixeiras:', p2); } main().finally(() => p.$disconnect());
