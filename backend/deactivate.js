const { PrismaClient } = require('@prisma/client'); const p = new PrismaClient(); async function main() { const nomes = ['D. Payet', 'A. Teixeira', 'Medel', 'Praxedes', 'Zé Gabriel', 'Rossi', 'Galdames', 'Clayton', 'Serginho']; const update = await p.jogador.updateMany({ where: { nomePopular: { in: nomes } }, data: { ativo: false } }); console.log('Inativados:', update.count); } main().finally(() => p.$disconnect());
