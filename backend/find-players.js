const { PrismaClient } = require('@prisma/client'); const p = new PrismaClient(); async function main() { const mutano = await p.jogador.findMany({ where: { nomePopular: { contains: 'Mutano', mode: 'insensitive' } } }); console.log('Mutano:', mutano); const jSilva = await p.jogador.findMany({ where: { nomeCompleto: 'João Vitor Silva' } }); console.log('J. Silva:', jSilva); } main().finally(() => p.$disconnect());
