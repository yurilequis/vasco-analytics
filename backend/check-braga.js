const { PrismaClient } = require('@prisma/client'); const p = new PrismaClient(); async function main() { const stats = await p.estatisticaJogador.findMany({ where: { partidaId: 38, equipe: { nome: { contains: 'Bragantino' } }, titular: true }, include: { jogador: true } }); stats.forEach(s => console.log(s.jogador.nomePopular + ' - ' + s.jogador.nomeCompleto + ' | Pos: ' + s.jogador.posicao)); } main().finally(() => p.$disconnect());
